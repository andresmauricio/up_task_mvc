doctype html
html(lang="en")
  head
    include partials/header
  body(class="login")
    .contenedor-formulario
      h1 Reestablecer contraseña
      p Agrega tu email para reestablecer la contraseña

      if locals.message
        div(class="alerta")
          if validates
            each category in Object.keys(validates)
              each error in validates[category]
                div(class=`${category}`)
                  p #{error}

      form(class="caja-login" method="POST" action="/recovery-password")
        .campo
          label(for="email") Email 
          input(type="email" name="email" placeholder="Ingresa tu correo") 
        .campo.enviar
          input(type="submit" value="Restablecer password" class="boton")
        .campo.acciones
          a(href="/login") Ya lo recorede, volver a login