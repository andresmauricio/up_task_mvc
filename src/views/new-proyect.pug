extends layout
include mixins/create-proyect
block content
  .contenido-principal
    h1 #{title}
    if errors
      each error in errors
        .alerta.error #{error.text}
    +createProject(detailProject)
    pre #{vardump(detailProject)}